#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mosca/bin/mosca" "$@"
  ret=$?
else 
  node  "$basedir/../mosca/bin/mosca" "$@"
  ret=$?
fi
exit $ret
